Library(['pipeline-utilities']) _
 
pipeline {
 // agent any
    agent {
        label 'jenkins-agent-maven'
    }
    //options {
      //  skipDefaultCheckout()
       // buildDiscarder(logRotator(numToKeepStr: '10'))
   // }
 
   // environment {
   //     EMAIL_RECIPIENTS = "Pauny.Tran@Ally.com"
   //     String gitCredentialsId="bitbucket-svc-readonly-ssh"
   //     String repoURL="ssh://git@bitbucket-dev.int.ally.com:7999/~kzt1vp/test-repo_new.git"
   // }
    stages {
        stage('Checkout') {
            steps {
                checkout scm
              echo "xxxxxxxxxxxxx"
               sh "mvn -version"
                //sh "getArtifact.sh"
             }
            }
        }  
}


